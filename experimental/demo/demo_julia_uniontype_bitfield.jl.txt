julia> unsafe_load(Ptr{UInt32}(typeof(uarr.ref.mem).layout))
0x00000008

julia> uarr_sz = unsafe_load(Ptr{UInt32}(typeof(uarr.ref.mem).layout))
0x00000008

julia> uarr_sel = Ptr{UInt8}(uarr.ref.mem.ptr + (uarr.ref.mem.length * uarr_sz))

Ptr{UInt8} @0x00000002ccd6db60

julia> uarr_sel + 1
Ptr{UInt8} @0x00000002ccd6db61

julia> unsafe_load(uarr_sel + 0)
0x01

julia> unsafe_load(uarr_sel + 1)
0x00

julia> uarr
2-element Vector{Union{Float64, Int64}}:
 1
 2.0

julia> typeof(uarr[1])
Int64

julia> typeof(uarr[2])
Float64

julia> 
